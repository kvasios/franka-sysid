name: franka-sysid
channels:
  - conda-forge
dependencies:
  # Ubuntu 24.04 ships Python 3.12 by default; this project currently targets Python 3.10.
  - python=3.10
  - pip
  # Optional convenience: keep Poetry inside the same env.
  - pip:
      - poetry==1.8.2

